# إعداد حساب المدير (Admin)

## الطريقة 1 — من الكود (موصى بها)

1. افتح `lib/config/admin_config.dart`
2. أضف بريدك الذي تسجّل به في Firebase:

```dart
const List<String> kBootstrapAdminEmails = [
  'me653830@gmail.com',
];
```

3. سجّل الدخول بهذا البريد في التطبيق → يُعيَّن تلقائياً `role: admin` و `canUpload: true`
4. ستظهر **لوحة الإدارة** في الشاشة الرئيسية

## الطريقة 2 — من Firebase Console

1. [Firebase Console](https://console.firebase.google.com) → مشروعك → **Authentication** → انسخ **UID** للمستخدم
2. **Firestore** → مجموعة `users` → مستند `{uid}`
3. عدّل الحقول:
   - `role` → `"admin"`
   - `canUpload` → `true`

## إضافة المقاطع للجميع (Admin) — روابط فقط

المقاطع تُضاف الآن **كروابط مباشرة** للملفات الصوتية (لا تُرفع ملفات داخل التطبيق — لتصغير حجمه).

- من **مزامير القرآن** → زر **إضافة مقطع برابط**
- اختر القارئ (أو أضف اسماً جديداً)، الصق الرابط، واكتب العنوان
- يُحفظ مستند خفيف فقط في **Firestore** (مجموعة `library_snippets`، الحقل `remoteUrl`) — بدون أي ملفات صوتية
- يظهر لجميع المستخدمين تحت **مقتطفات بصائر** ويُشغَّل بالبث المباشر

**مهم:** يجب أن يكون الرابط **مباشراً** لملف صوتي (ينتهي بـ `.mp3` / `.m4a` / `.m3u8`).
روابط صفحات مثل `soundcloud.com/...` أو `youtube.com/...` **لا تعمل** — يحتاجها المشغّل كملف صوتي مباشر.
استضف الملفات على مكان يعطي رابطاً مباشراً (مثل archive.org أو Firebase Storage أو أي خادم).

## قواعد Firestore (مهم — مجاني)

انشر ملف `firestore.rules` فقط:

Firebase Console → **Firestore Database** → **Rules** → الصق المحتوى → **Publish**

لا حاجة لـ **Storage** أو ملف `storage.rules`.

## تفعيل الإضافة لمستخدمين آخرين

1. سجّل دخولك كمدير
2. **لوحة الإدارة** → **إدارة صلاحية الرفع**
3. فعّل المفتاح بجانب أي مستخدم (غير المدير)

> ملاحظة: الكتابة في المكتبة المشتركة محصورة في حساب **المدير** (role == admin) حسب قواعد Firestore الحالية.
